# Copy to .streamlit/secrets.toml (local) or paste into the Streamlit Cloud "Secrets" box.
# Direct download URLs of the release assets (Assets -> right-click -> copy link address).
MASTER_PARQUET_URL = "https://github.com/<user>/<repo>/releases/download/<tag>/battery_master_data.parquet"
IMPEDANCE_PARQUET_URL = "https://github.com/<user>/<repo>/releases/download/<tag>/impedance_ground_truth.parquet"
